<h3 id='title'>パスワードを忘れた方</h3>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <%= devise_error_messages! %>

  <div class="field">
    <%= f.label :name, '名前'%><br />
    <%= f.text_field :name, autofocus: true %>
  </div>

  <br>

  <%= render "devise/shared/links" %>

  <div class="submit">
    <%= f.submit "パスワードリセット" %>
  </div>
<% end %>
